## wmctl connect

Connect as an ephemeral node in the cluster

```
wmctl connect [flags]
```

### Options

```
      --force-tun               force the use of a TUN interface
  -h, --help                    help for connect
      --insecure                do not use TLS when joining the cluster
      --interface-name string   name of the wireguard interface to use (default "webmesh0")
      --join-server string      address of the join server to use
      --listen-port uint16      port for wireguard to listen on (default 51820)
      --local-dns               start a local MeshDNS server
      --local-dns-port uint16   port to use for the local MeshDNS server (default 5353)
      --log-level string        log level to use (default "info")
      --modprobe                attempt to load the wireguard kernel module
      --no-ipv4                 do not use IPv4 when joining the cluster
      --no-ipv6                 do not use IPv6 when joining the cluster
      --raft-port uint16        port to use for the Raft transport (default 9443)
      --tls-ca-file string      path to a CA file for verifying the join server's certificate
      --tls-cert-file string    path to a TLS certificate file to use for mTLS
      --tls-key-file string     path to a TLS key file to use for mTLS
      --verify-chain-only       only verify the join server's certificate chain
```

### Options inherited from parent commands

```
      --basic-auth-password func     The password for basic authentication
      --basic-auth-username func     The username for basic authentication
      --certificate-authority func   The path to the CA certificate for the cluster connection
      --client-certificate func      The path to the client certificate for the user
      --client-key func              The path to the client key for the user
  -c, --config string                Path to the CLI configuration file
      --context string               The name of the context to use (default "flags")
      --ldap-password func           The password for LDAP authentication
      --ldap-username func           The username for LDAP authentication
      --prefer-leader                Whether to prefer the leader node for the cluster connection
      --server string                The URL of the node to connect to
      --tls-skip-verify              Whether TLS verification should be skipped for the cluster connection
```

### SEE ALSO

* [wmctl](wmctl.md)	 - wmctl is a CLI tool for managing a webmesh

###### Auto generated by spf13/cobra on 23-Aug-2023
